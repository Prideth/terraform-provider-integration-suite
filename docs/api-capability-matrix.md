# API Capability Matrix

Scope: SAP Cloud Integration's public "Integration Content", "Security Content", and
"Partner Directory" OData V2 APIs (published under the `CloudIntegrationAPI` package on
SAP Business Accelerator Hub), plus the Access Policy API. A Terraform column of "Resource"
is only used where Create, Read, and a stable identity are all realistic; "Unsupported" is
used whenever a Terraform resource would not be able to fulfil its contract (see
`resource-design.md` for the full suitability check per object). The Number Range row is a
deliberate, narrow exception: SAP documents no GET for that entity at all, so it is a
"write-only lifecycle" resource — Create and Update are real, but Read is a documented no-op
and Import/Delete both refuse explicitly rather than guessing — see `resource-design.md` and
`guides/runtime-stores-and-number-ranges.md` for why this is still judged safer than either
skipping the resource entirely or inventing an unconfirmed GET/DELETE.

| Domain | Object | Runtime | Public API | Protocol | GET | CREATE | UPDATE | DELETE | DEPLOY | Terraform |
|---|---|---|---|---|---|---|---|---|---|---|
| Integration Content | Integration Package | Design-time | Yes | OData V2 | Yes | Yes | Partial (metadata only; SAP-provided packages are read-only) | Yes | N/A | Resource + Data Source |
| Integration Content | Integration Flow (Designtime Artifact) | Design-time | Yes | OData V2 | Yes | Yes (new version) | Yes (new version, versioned not in-place) | Yes | via `DeployIntegrationDesigntimeArtifact` action | Resource + Data Source |
| Integration Content | Integration Runtime Artifact | Runtime | Yes | OData V2 | Yes | N/A (created by Deploy) | N/A | Yes (undeploy) | N/A | Declarative Deployment Resource |
| Integration Content | Value Mapping (Designtime Artifact) | Design-time | Yes | OData V2 | Yes | Yes | No — resolved conservatively this phase: no confirmed in-place update, so `name`/`content`/`content_hash` are `RequiresReplace` instead of an unverified `PUT`; see `sap-api-references.md` (SAP separately documents a distinct `ValueMappingDesigntimeArtifactSaveAsVersion` action, deferred to v0.2.x pending confirmation) | Yes | via `DeployValueMappingDesigntimeArtifact` action (singular form re-checked this phase) | Resource + Data Source |
| Integration Content | Value Mapping Entries (`UpsertValMaps`/`UpdateDefaultValMap`/`DeleteValMaps`) | Design-time | Yes | OData V2 | Partial (`ValMapId` lookup only, via a separate `ValueMappingDesigntimeArtifacts` read — not returned by `UpsertValMaps` itself) | Yes (into an existing scheme only — 404 otherwise; query-parameter-only POST, no request body) | Yes (`UpdateDefaultValMap`) | Yes (granularity unconfirmed) | N/A | Unsupported — exact payload/path shapes not confirmed against a reachable primary source, see resource-design.md |
| Integration Content | Script Collection (Designtime Artifact) | Design-time | Yes | OData V2 | Yes | Yes | Yes, via `PUT` (new version, versioned not in-place) — confirmed via SAP's own documentation plus a third-party client's API coverage, the same evidence used for Message Mapping; see `sap-api-references.md` | Yes (scope unconfirmed — see `sap-api-references.md`) | via `DeployScriptCollectionDesigntimeArtifact` action (singular form) | Resource + Data Source |
| Integration Content | Message Mapping (Designtime Artifact) | Design-time | Yes | OData V2 | Yes | Yes | Yes, via `PUT` (new version, versioned not in-place) — confirmed via SAP's own documentation plus a third-party client's API coverage, unlike Value Mapping's `PUT`; see `sap-api-references.md` | Yes (scope unconfirmed — see `sap-api-references.md`) | via `DeployMessageMappingDesigntimeArtifact` action (singular form) | Resource + Data Source |
| Integration Content | Integration Adapter (custom, Cloud Foundry only) | Design-time | Yes (confirmed for Delete/Deploy only; Create/Read corroborated by analogy — see `sap-api-references.md`) | OData V2 | Unconfirmed by an adapter-specific example (standard GET-by-key assumed) | Corroborated by analogy to sibling artifact types, not SAP-confirmed for this entity | No — duplicate-ID-is-an-error is positive evidence against a working reimport/update flow | Yes, confirmed (`Id` alone, not `(Id, Version)`) | via `DeployIntegrationAdapterDesigntimeArtifact` action (singular, confirmed, no `Version` parameter — unlike every sibling deploy action) | Resource + Data Source — see `sap-api-references.md` and `guides/integration-adapters.md` |
| Integration Content | Integration Adapter Deployment | Runtime | Yes (Deploy confirmed; runtime status/undeploy reused from the shared `IntegrationRuntimeArtifacts` entity by analogy, not independently confirmed for adapters) | OData V2 | Yes (assumed via shared `IntegrationRuntimeArtifacts`) | N/A (created by Deploy) | N/A | Yes (assumed via shared `IntegrationRuntimeArtifacts`, unconfirmed for adapters specifically) | N/A | Declarative Deployment Resource — no `*_version` attribute, unlike every sibling deployment resource (see `sap-api-references.md`) |
| Integration Content | Service Endpoints | Runtime | Yes | OData V2 | Yes (read-only, `$expand=EntryPoints,ApiDefinitions`, `$filter=Name`/`Protocol`, server-driven paging) | No | No | No | N/A | Data Source — see `sap-api-references.md` and `guides/service-endpoints.md` |
| Integration Content | Custom Tag Configuration (tenant-wide singleton) | Design-time / tenant config | Yes, confirmed verbatim (Create, Overwrite, Read all confirmed with exact example payloads) | OData V2 | Yes (`GET .../CustomTagConfigurations('CustomTags')/$value`, raw `$value` body, not the standard OData envelope) | Yes (`POST .../CustomTagConfigurations`) | Yes, same operation as Create with `Overwrite=true` (confirmed) | No — reverified, no delete/clear operation documented anywhere for this entity | N/A | Resource + Data Source — Delete returns an explicit error rather than a guessed clearing mechanism; see `sap-api-references.md` and `guides/custom-tag-configurations.md` |
| Integration Content | Message Processing Logs | Runtime | Yes | OData V2 | Yes (read-only) | No | No | No | N/A | Unsupported — monitoring data, not infrastructure state (see provider-scope.md §66) |
| Integration Content | Number Range | Design-time (write-only) | Yes (Create, Update confirmed; no GET, no DELETE documented anywhere) | OData V2 | No — confirmed absent, see `sap-api-references.md` | Yes | Yes (PUT; `CurrentValue` omitted unless explicitly requested) | No — confirmed absent; UI shows "Undeploy", not "Delete", with no REST equivalent | N/A (UI-only "Runtimes" field has no API counterpart) | Resource — write-only lifecycle: Read is a documented no-op, Import and Delete both return explicit errors; see `sap-api-references.md` and `guides/runtime-stores-and-number-ranges.md` |
| Integration Content | Variable | Runtime | Yes (one GET only: `Variables(...)/$value`) | OData V2 | Yes (single item by composite key only; no collection GET, no metadata fields) | No | No | No (unconfirmed; permission template exists but no REST example found) | N/A | Unsupported — no creation API at all; the one confirmed read returns only the raw runtime value with no safer metadata-only alternative, see `provider-scope.md` |
| Integration Content | Data Store | Runtime | Yes (one GET only: `DataStores?overdueonly=true`) | OData V2 | Yes (aggregate monitoring counts only) | No (implicit runtime container) | No | No | N/A | Unsupported — no independent creation API; the one GET is monitoring data, same class as Message Processing Logs |
| Integration Content | Data Store Entry | Runtime | Yes (GET only: single entry, all entries for a store) | OData V2 | Yes | No | No | No (design-time flow step only, not a REST call) | N/A | Out of scope — runtime business message data (Status/MessageId/payload-adjacent timestamps), not infrastructure state |
| Security Content | User Credentials | Design-time | Yes | OData V2 | Yes (metadata only; secret values are never returned) | Yes | Yes, via `PUT` (full redeploy) | Yes | N/A | Resource + Data Source (`write-only` password) |
| Security Content | OAuth2 Client Credentials | Design-time | Yes | OData V2 | Yes (metadata only) | Yes | Yes, via `PUT` (full redeploy) | Yes | N/A | Resource + Data Source (`write-only` client secret; only name/description/token_service_url/client_id/scope exposed, see `sap-api-references.md`) |
| Security Content | Keystore Entry (certificates, key pairs, any keyed entry) | Design-time | Yes, confirmed (GET collection and by hex alias, confirmed field-for-field) | OData V2 | Yes | No | No | No | N/A | Data Source only — no field distinguishes SAP-owned from tenant-owned entries, and this entity spans multiple fundamentally different object types; see `security-content.md` guide |
| Security Content | Certificate (standalone) | Design-time | Yes, confirmed (Create/Update share one PUT `.../$value` operation, confirmed with SAP's own documented example) | OData V2 | Yes | Yes | Yes | Yes (via shared `KeystoreResources` mass-delete, one alias) | N/A | Resource + read-back via the Keystore Entry data source — drift detection uses a locally-computed SHA-256 fingerprint of the certificate's DER bytes, not raw PEM text, see `security-content.md` guide |
| Security Content | Key Pair (SAP-generated) | Design-time | Yes, confirmed (`POST KeyPairGenerationRequests`, full field table confirmed against SAP's own documentation) | OData V2 | Partial (only `KeyType`/`KeySize`/`ValidNotBefore`/`ValidNotAfter` confirmed returned by `KeystoreEntries`; `SignatureAlgorithm`/DN fields are not) | Yes | No — no update operation documented; every generation-defining attribute is `RequiresReplace` | Yes (via shared `KeystoreResources` mass-delete, one alias) | N/A | Resource (`partial` — see the GET column) — private key material is never requested or exposed, see `security-content.md` guide |
| Security Content | Keystore (whole, `KeystoreResources`) | Design-time | Yes, confirmed (`POST` import/backup with base64 JKS/JCEKS + password; `PUT ...?deleteEntries=true` mass-delete) | OData V2 | N/A | Yes (whole-keystore import) | N/A | Yes (mass-delete, arbitrary alias list) | N/A | Deliberately out of scope — a single import/delete call can affect many entries at once regardless of which Terraform module or administrator owns them, incompatible with granular resource ownership; see `provider-scope.md`. The confirmed mass-delete operation is reused internally, with exactly one alias, by Certificate and Key Pair Delete |
| Security Content | SSH Key | Design-time | No independent entity — reverified and corrected: SAP's own API overview lists no "SSH Key" resource, and the UI documents identical Key Pair semantics for both | N/A | N/A | N/A | N/A | N/A | N/A | Covered by Key Pair's `public_key_openssh` (confirmed `KeystoreEntries('<hex>')/Sshkey/$value` export for RSA/DSA), no separate resource — see `security-content.md` guide |
| Security Content | Certificate Chain | Design-time | Reverified: documented only as a capability of the Key Pair resource ("create a CSR, or import and export the related certificate chain"), no independent entity-set example found | Unknown | Unknown | Unknown | Unknown | Unknown | N/A | Unsupported — research required; likely scoped by key-pair alias if confirmed later |
| Security Content | Certificate-User Mapping | Design-time | No (Neo environment only; no Cloud Foundry equivalent found) | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no public API for this provider's Cloud Foundry target, see `security-content.md` guide |
| Security Content | Secure Parameter | Design-time | Unconfirmed — reverified: listed conceptually in SAP's API overview, but zero worked examples in the curated example-requests index and only Eclipse-wizard UI docs found | Unknown | Unknown | Unknown | Unknown | Unknown | N/A | Unsupported — research required |
| Security Content | Known Hosts (SSH) | Design-time | No — reverified and strengthened: does not appear in SAP's API overview's resource table at all, UI-only documentation found | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no public API found |
| Security Content | OAuth2 Authorization Code | Design-time | Yes (UI-documented) | OData V2 | Unknown | Unknown | Unknown | Unknown | N/A | Out of scope — requires interactive human authorization, incompatible with Terraform's non-interactive apply model |
| Security Content | Access Policies | Design-time | Yes — Security Content API, `AccessPolicies` keyed by Edm.Int64; contract confirmed from SAP's own CI/CD tooling (re-audited 2026) | OData V2 | Yes (by ID, or `$filter` on `RoleName`) | Yes | Yes (PUT with `RoleName` + `Description`) | Yes (also removes the policy's references) | N/A | Resource + Data Source |
| Security Content | Access Policy Artifact References | Design-time | Yes — top-level `ArtifactReferences` set, bound to a policy via the `AccessPolicy` navigation property (re-audited 2026) | OData V2 | Yes (through `AccessPolicies(<id>L)/ArtifactReferences`) | Yes | No public contract (UI-only edit; SAP's tooling deletes and recreates) | Yes | N/A | Resource + Data Source — every attribute `RequiresReplace`, see resource-design.md |
| Partner Directory | Partner (`Partners`) | Design-time | Yes (read-only: no confirmed create; Pid comes into existence implicitly via a child entity) | OData V2 | Yes | No (unconfirmed) | N/A | Cascades to child entities (documented) | N/A | Data Source only — see `resource-design.md` |
| Partner Directory | String Parameter | Design-time | Yes | OData V2 | Yes | Yes | Yes, via `PUT` (full replace) | Yes | N/A | Resource + Data Source |
| Partner Directory | Binary Parameter | Design-time | Yes | OData V2 | Yes | Yes | Yes, via `PUT` (full replace) | Yes | N/A | Resource + Data Source |
| Partner Directory | Alternative Partner | Design-time | Yes | OData V2 | Yes | Yes | Yes, via `PUT` (repoints `Pid`) | Yes | N/A | Resource + Data Source — key is hex-encoded (`Hexagency`/`Hexscheme`/`Hexid`), hidden by this provider |
| Partner Directory | Authorized User | Design-time | Yes | OData V2 | Yes | Yes | Yes, via `PUT` (repoints `Pid`) | Yes | N/A | Resource + Data Source |
| Partner Directory | User Credential Parameter | Design-time | Yes | OData V2 | Yes (identity only; no password read-back confirmed) | Yes | No confirmed in-place update | Yes | N/A | Resource (`write-only` password, no Update; cannot batch with other entity types in one ChangeSet) |
| Classic API Management | API Provider | Design-time | Yes (API Portal `Management.svc`, confirmed field-for-field from SAP's official user guide) | OData V2 | Yes | Yes | No (SAP's own Piper tooling documents create-only) | Yes | N/A | Resource + Data Source — Internet connection type only, every attribute `RequiresReplace` |
| Classic API Management | API Product | Design-time | Yes (confirmed verbatim Create/Update worked examples) | OData V2 | Yes | Yes | Yes (narrower field set than Create — confirmed, no `apiProxies` update) | Yes (inferred from this API family's consistent convention) | N/A | Resource + Data Source |
| Classic API Management | API Product Additional Property | Design-time | Yes (confirmed verbatim Create/Update worked examples, composite key) | OData V2 | Implicit (embedded in product read) | Yes | Yes | Not shown verbatim | N/A | Managed as part of `sapintegrationsuite_api_product`'s `additional_properties`, not a separate resource |
| Classic API Management | Certificate Store Reference | Design-time | Yes (confirmed verbatim Create/Read/Update/Delete worked examples — the best-confirmed object in this API family) | OData V2 | Yes | Yes | Yes | Yes | N/A | Resource + Data Source |
| Classic API Management | Key Value Map | Design-time | Yes (Create confirmed verbatim; Update/Delete UI-confirmed to exist, no REST payload shown) | OData V2 | Yes | Yes | No (unconfirmed per-entry payload) | Yes (inferred from this API family's consistent convention) | N/A | Resource + Data Source — unencrypted maps only, entries `RequiresReplace` |
| Classic API Management | API Proxy | Design-time & Runtime | Yes (entity, GET, DELETE confirmed; ZIP bundle structure confirmed from SAP's own sample repository) | OData V2 | Yes | Not confirmed (ZIP content upload wire format unknown) | N/A | Yes (inferred) | Unconfirmed | Unsupported — Create mechanism not confirmed, see `sap-api-references.md` |
| Classic API Management | API Proxy Policy | Design-time | Confirmed to be XML content embedded in the API Proxy ZIP bundle, not an independent entity | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — not independently addressable; would be managed as part of API Proxy content |
| Current API Management | API Artifact | Design-time | No — confirmed absent from the exhaustive Integration Content API resource table, and no REST/OData mention found anywhere across every artifact-lifecycle page checked | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no public API found; see `sap-api-references.md` and `guides/current-api-management.md` |
| Current API Management | API Artifact Deployment | Runtime | No — same exhaustive search as above found no deployment/undeploy API | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no public API found |
| Current API Management | API Policy | Design-time | No — no independent entity found; policies are described only as part of the UI-configured artifact | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no public API found; ownership model (nested vs. independent) could not be determined without one |
| Current API Management | Reusable API Artifact | Design-time | No — same absence as API Artifact | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no public API found |
| Current API Management | Runtime Profile | Design-time (UI-only concept) | No — configured under *Settings* > *Integrations*, no API mentioned | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no public API found. Even if one existed, the profile list is small, stable, human-documentable data, not a strong data-source candidate on its own |
| Current API Management | Integration Cell Runtime | Runtime | No — activation and status are both `Settings`/`Monitor` UI-only | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — manual bootstrap required, see `provisioning-capability-matrix.md` |
| Current API Management | Integration Cell Virtual Host | Design-time | No — management confirmed as `PI_Administrator`-gated UI only (*Monitor* > *Manage Virtual Host*) | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no public API found |
| Edge Integration Cell | Registration | Runtime bootstrap | No — a UI wizard plus a standalone Edge Lifecycle Management Bridge executable performing a direct Kubernetes handshake, not an HTTP API | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no public API found; see `guides/edge-integration-cell.md` |
| Edge Integration Cell | Local API Access (Message Processing Logs, Message Stores) | Runtime | Yes — confirmed reachable at `/local/api/v1`, documented on SAP Business Accelerator Hub | OData V2 | Yes | N/A | N/A | N/A | N/A | Unsupported, `out_of_scope` — monitoring/runtime data, same category as Cloud Integration's own Message Processing Logs |
| Edge Integration Cell | Operations Cockpit (Component/RuntimeParameter/Job) | Runtime | Yes — confirmed reachable at `/local/api/eic/v1` | OData V4 | Yes | Partial (RuntimeParameter update documented) | Partial | N/A | N/A | Unsupported, `out_of_scope` — configures Kubernetes-level pod resources for SAP-operator-managed components, and exposes an imperative restart action |
| Edge Integration Cell | Deployment Targeting | Design-time | No confirmed API parameter found in any Deploy action this provider calls | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no confirmed API found; see `guides/edge-integration-cell.md` |
| Edge Integration Cell | Access Policy Replication | Design-time | Partially — the public `AccessPolicies` entity has an `AccessPolicyRuntimeAssignments` navigation property, but its schema and write semantics are not published | OData V2 | Unknown | Unknown | Unknown | Unknown | N/A | Unsupported, `public_api_incomplete` |
| Integration Assessment | Master Data (Domain/Style/Use Case Pattern/Key Characteristic family) | Reference data | Yes — confirmed service and entity list, no confirmed field-level schema | Unconfirmed | Unconfirmed | Unconfirmed | Unconfirmed | Unconfirmed | N/A | Unsupported, `research_required`; see `guides/integration-assessment.md` |
| Integration Assessment | Landscape Configuration (Application/Technology/Vendor family) | Design-time | Yes — confirmed service, entity list, and per-tenant limits; no confirmed field-level schema | Unconfirmed | Unconfirmed | Unconfirmed | Unconfirmed | Unconfirmed | N/A | Unsupported, `research_required` — the strongest candidate in this capability once a schema is confirmed |
| Integration Assessment | Requests and Assessment Workflow | Workflow | Yes — confirmed service and entity list, no confirmed field-level schema | Unconfirmed | Unconfirmed | Unconfirmed | Unconfirmed | Unconfirmed | N/A | Unsupported, `out_of_scope` — workflow/project state regardless of schema confirmation |
| Trading Partner Management | Company/Trading Partner/Communication Partner Profile, Agreement Template, Agreement | Design-time | No — no API-access documentation page found anywhere in ~90 pages checked; export/import is UI-only JSON download | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no public API found; see `guides/trading-partner-management.md` |
| Trading Partner Management | Partner Directory generation (on agreement activation) | Runtime side effect | Confirmed UI-triggered push into Partner Directory, no independent API | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported, `out_of_scope` — imperative action; the generated Partner Directory entries are already managed directly via existing resources |
| Integration Advisor | MIG/MAG/Type System/Codelist/Shared Code/Global Parameters | Design-time | No — no API-access documentation page found across ~85 pages; one plausible-looking OAuth page turned out to describe Cloud Integration access, not this capability's own content | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no public API found; see `guides/integration-advisor.md` |
| Integration Advisor | Runtime Artifact Injection (into Cloud Integration) | Design-time action | Confirmed UI wizard, no documented REST equivalent | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported, `out_of_scope` — imperative action; target integration flow content is already managed directly via existing resources |
| Migration Assessment | Source System | Design-time | No — no API-access documentation page found; Migration Assessment consumes the source system's own SAP PO APIs, does not expose one for itself | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported — no public API found; see `guides/migration-assessment.md` |
| Migration Assessment | Data Extraction Request / Scenario Evaluation Request / results | Workflow & reporting | No confirmed API; action-triggered with a resulting status, and reporting output | N/A | N/A | N/A | N/A | N/A | N/A | Unsupported, `out_of_scope` — would remain out of scope even with a confirmed API |
| API Composition | Business Data Graph | Design-time | Yes — confirmed verbatim Create/Read/Update worked examples, Delete stated to exist | REST/OData (Configuration API) | Yes | Yes | Yes | Stated, not shown | N/A | Unsupported, `not_implemented` — strongest confirmed-but-unimplemented finding in this catalog; see `guides/classic-api-management.md`-style future phase recommendation in ROADMAP.md |
| OData Provisioning | Registered OData Service | Design-time | Positive signal only (`ODPAPIAccess` role documented) — no worked example found | Unconfirmed | Unconfirmed | Unconfirmed | Unconfirmed | Unconfirmed | N/A | Unsupported, `research_required` |
| Event Mesh | Queues, Topic/Webhook Subscriptions | Runtime configuration | Yes — genuine, well-documented Solace PubSub+-based broker management API | AMQP/MQTT/REST (Solace) | Yes | Yes | Yes | Yes | N/A | Unsupported, `separate_provider` — belongs to a different provider's boundary by design |
| Data Space Integration | Connector, Asset, Policy, Contract Definition/Negotiation/Agreement | Design-time & workflow | Yes — confirmed dedicated service instance and OData REST API package on Business Accelerator Hub, unreachable without an SAP login | OData | Unconfirmed | Unconfirmed | Unconfirmed | Unconfirmed | N/A | Unsupported, `research_required` — flagged for a future dedicated phase |
| Open Connectors | Connector instance (170+ types) | Design-time | Yes, per-connector-type | REST (per connector, OpenAPI-documented) | Yes | Yes | Yes | Yes | N/A | Unsupported, `out_of_scope` — deliberate scope judgment, not a research gap |

Rows marked "Unsupported (v0.1.x)" are real API surfaces that a future minor version can
reasonably add; rows in `provisioning-capability-matrix.md` marked "Unsupported – manual
bootstrap required" are gaps with no known public API at all.
